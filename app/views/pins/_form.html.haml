=simple_form_for @pin, html: {multipart:true} do |f|
  - if @pin.errors.any?
    #errors
    %h2
    = pluralize(@pin.errors.count, "error")
    prevented this Pin from Saving
    %ul
      - @pin.errors.full_messages.each do |msg|
        %li=msg
  .form_group
  = f.input :image, input_html: {class: 'form_control', input_type:'file', accept: 'image/*', capture: 'camera'} 
  
  .form_group
  = f.input :title, input_html: {class: 'form_control'}
  
  .form_group
  = f.input :tag_list, input_html: {class: 'form_control', placeholder: 'Comma Seperated'}
  
  .form_group
  = f.input :description, input_html: {class: 'form_control'}
  
  = f.button :submit, class: "btn btn-primary"